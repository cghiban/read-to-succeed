<!DOCTYPE html>
<html lang="en">
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1">
        <link rel="stylesheet" href="/static/styles.css">
        <title>Reading Log - Read to Succeed</title>
    </head>
  <body>

  {{ block "menu_burger" .}}{{end}}

  <h1>Reading Log</h1>

  <div class="sub-nav">
      <select id="readers">
  {{- $cr := .CurrentReader -}}
      <option value="">-= pick a reader =-</option>
  {{ range .Readers -}}
      <option value="{{.Name}}" {{if eq .Name $cr}}selected{{end}}>{{.Name}}</option>
  {{ end }}
      </select>
      <a href="#" id="addentry" title="add reading">add entry <button> + </button></a>
      <a href="#" id="togglestats" title="stats">show totals</a>
  </div>

    <div id="stats" hidden>
    Totals:
      <ul>
      {{ range .Stats -}}
        <li><label>{{ .ReaderName }}</label> {{ .TotalDuration }}</li>
      {{ end }}
      </ul>
      <div><a href="/dailystats">View last 30 days</a></div>
    </div>

<div style="overflow-x:auto;" id="content">
  <table id="readinglist">
    <thead>
    <tr>
      <th>Reader</th>
      <th>Author</th>
      <th>Title</th>
      <th>Note</th>
      <th>Duration</th>
      <th>Pages</th>
      <th>Day</th>
      <th>Added on</th>
    </tr>
    </thead>
    <tbody>
  {{ range .Readings}}
  <tr>
      <td>{{.ReaderName}}</td>
      <td>{{.BookAuthor}}</td>
      <td>{{.BookTitle}}
        <a href="#" id="addentry" data__reader="{{.ReaderName}}" data__author="{{.BookAuthor}}" data__title="{{.BookTitle}}">
          <button> + </button>
        </a>
      </td>
      <td>{{.Note}}</td>
      <td>{{.Duration}}m</td>
      <td>{{.Pages}}</td>
      <td>{{dayToDate .Day}}</td>
      <td>{{dateISOish .CreatedOn}}</td>
  </tr>
  {{ end }}
  <tbody>
  </table>
  {{block "pagination" .}}{{end}}
</div>

    <!-- The Modal -->
    <div id="myModal" class="modal">
      <!-- Modal content -->
      <div class="modal-content">
        <span class="close">&times;</span>
        <h3>Add new reading</h3>
        <form id="addreading">
          <div>
            <label>Reader</label>
            <!--<input type="text" name="reader" readonly required>-->
            <select name="reader" required>
              <option value="">-= pick a reader =-</option>
              {{ range .Readers}}
              <option value="{{.Name}}">{{.Name}}</option>
              {{ end}}
            </select>
          </div>
          <div>
            <label>Book Author</label>
            <input type="text" name="author" required>
          </div>
          <div>
            <label>Book Title</label>
            <input type="text" name="title" required>
          </div>
          <div>
            <label>Date</label>
            <input type="date" name="day" required value="{{.Today}}">
          </div>
          <div>
            <label>Duration</label>
            <input type="number" min="0" max="90" name="duration"> <small>(minutes)</small>
          </div>
          <div>
            <label>Pages</label>
            <input type="number" min="0" max="100" name="pages">
          </div>
          <div>
            <a href="#" id="show_note_box" onclick='document.querySelector("#notecontainer").style.display="block";this.parentNode.style.display="none";'>add a note</a>
          </div>
          <div id="notecontainer" style="display:none;">
            <label style="vertical-align: top;">Note</label>
            <textarea name="note"></textarea>
          </div>
          <div>
            <label></label>
            <button>submit</button>
          </div>
        </form>
      </div>

    </div>

  <script type="text/javascript" src="/static/scripts.js"></script>
  </body>
</html>
